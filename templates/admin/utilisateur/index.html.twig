{% extends 'admin/admin.html.twig' %}

{% block title "Administration" %}


{% block body %}
<div class="text">Administration des utilisateurs</div>
<div class="row">
<table class="table table-dark bdr">
    <thead>
    <tr>
        <th>Email</th>
        <th>Rôle</th>
        <th>Actions</th>
    </tr>
    </thead>
    <tbody>
    {% for utilisateur in user %}
    <tr>
        <td>  
                {{ utilisateur.email | default('') }}</a>
        </td>
        <td> {% for roles in utilisateur.roles %}
            {{ roles }} || 
            {% endfor %} 
        </td>
        <td>    
            <div class="d-flex gap-1">
            <li class="action1">
              <a class="edit" href="{{ path('app_admin_utilisateur_edit', { id: utilisateur.id }) }}">
                  <i class='bx bxs-wrench' style="align-self:center"></i>
              </a>
            </li>
                <form class="action" action="{{ path('app_admin_utilisateur_delete' , {id: utilisateur.id}) }}" method="post">
                    <input type="hidden" name="_method" value="DELETE">
            <li class="action2">
              <button class="action" type="submit">
                  <i class='bx bxs-trash'></i>
              </button>
              </a>
            </li>
                </form>          
           </div>
        </td>
    {% endfor %}
    </tbody>
    </table>
</div>
{% endblock %}