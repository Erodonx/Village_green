{% extends 'admin/admin.html.twig' %}

{% block title "Administration" %}

{% block body %}
<div class="text">Administration des commandes fournisseurs</div>
<div class="row">
<table class="table table-dark bdr">
    <thead>
    <tr>
        <th>Id fournisseur</th>
        <th>Nom du fournisseur</th>
        <th>Type du fournisseur</th>
        <th>Actions</th>
    </tr>
    </thead>
    <tbody>
    {% for fournisseur in fournisseurs %}
    <tr>
        <td>  
        <a class="id" href="">{{ fournisseur.id }}</a>
        </td>
        <td>{{ fournisseur.nom | default('') }}</td>
        <td>{{ fournisseur.type}}</td>
        <td>         <div class="d-flex gap-1">
            <li class="action1">
              <a class="edit" href="{{ path('app_admin_fournisseur_edit', { id: fournisseur.id }) }}">
                  <i class='bx bxs-wrench' style="align-self:center"></i>
              </a>
            </li>
                <form class="action" action="{{ path('app_admin_fournisseur_delete' , {id: fournisseur.id}) }}" method="post">
                    <input type="hidden" name="_method" value="DELETE">
            <li class="action2">
              <button class="action" type="submit">
                  <i class='bx bxs-trash'></i>
              </button>
              </a>
            </li>
                </form>          
           </div></td> 
    </tr>
    {% endfor %}
    </tbody>
    </table>
<p>
    <a class="btn btn-primary btn-sm" href="{{ path('app_admin_fournisseur_create') }}">Créer un nouveau fournisseur</a>
</p>
</div>
{% endblock %}

