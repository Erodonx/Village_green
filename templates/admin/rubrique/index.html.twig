{% extends 'admin/admin.html.twig' %}

{% block title "Administration" %}


{% block body %}
<div class="text">Administration des rubriques</div>
<div class="row">
<table class="table table-dark bdr">
    <thead>
    <tr>
        <th>Titre</th>
        <th>Actions</th>
    </tr>
    </thead>
    <tbody>
    {% for rubrique in rubriques %}
    <tr>
        <td>  
            <a class="libelle" href="{{ path('app_admin_rubrique_edit', {id: rubrique.id}) }}" style="text-decoration:none">{{ rubrique.getNom }}</a>
        </td>
        <td>    
                   <div class="d-flex gap-1">
            <li class="action1">
              <a class="edit" href="{{ path('app_admin_rubrique_edit', { id: rubrique.id }) }}">
                  <i class='bx bxs-wrench' style="align-self:center"></i>
              </a>
            </li>
                <form class="action" action="{{ path('app_admin_rubrique_delete' , {id: rubrique.id}) }}" method="post">
                    <input type="hidden" name="_method" value="DELETE">
            <li class="action2">
              <button class="action" type="submit">
                  <i class='bx bxs-trash'></i>
              </button>
              </a>
            </li>
                </form>          
           </div>
    </tr>
    {% endfor %}
    </tbody>
    </table>
<p>
    <a class="btn btn-primary btn-sm" href="{{ path('app_admin_rubrique_create') }}">Créer une nouvelle Rubrique</a>
</p>
</div>
{% endblock %}
