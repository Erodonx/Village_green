{% extends 'admin/admin.html.twig' %}

{% block title "Administration" %}

{% block body %}
<p>
    Une description des différents atouts du site internet ||| Une descrption des différents atouts du site internet ||| Une description des différents atouts du site internet ||| Une description des différents atouts du site internet ||| Une description des différents atouts du site internet ||| Une description des différents atouts du site internet ||| Une description des différents atouts du site internet ||| Une description des différents atouts du site internet ||| 
</p>
<p>
    <a class="btn btn-primary btn-sm" href="{{ path('app_admin_produit_create') }}">Créer un nouveau produit</a>
</p>
<table class="table">
    <thead>
    <tr>
        <th>Titre</th>
        <th>Categorie</th>
        <th>Actions</th>
    </tr>
    </thead>
    <tbody>
    {% for produit in produits %}
    <tr>
        <td>  
            <a href="{{ path('app_admin_produit_show', { id: produit.getId , slug:produit.getSlug }) }}">{{ produit.getNom }}</a>
        </td>
        <td>{{ produit.categorie.nom | default('') }}</td>
        <td> 
            <div class="d-flex gap-1">
                <a class="btn btn-primary btn-sm" href="{{ path('app_admin_produit_edit', { id: produit.id }) }}">Éditer</a>
                <form action="{{ path('app_admin_produit_delete' , {id: produit.id}) }}" method="post">
                    <input type="hidden" name="_method" value="DELETE">
                    <button type="submit" class="btn btn-danger btn-sm">Supprimer</button>
                </form>          
           </div>
        </td>
    {% endfor %}

{% endblock %}
