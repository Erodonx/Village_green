{% extends 'base.html.twig' %}
{% block title "Produit : " ~ produit.nom %} {# Pas besoin de mettre un endblock si ça tient sur une seule ligne #}
{% block body %}

 <div class="container mt-4">
    <div class="card bg-white">
    <div class="card-title d-flex justify-content-center"><p class="desc-title">{{produit.nom}}</p></div>
    <div class="card-body"> 
        <div class="row justify-content-center"><img class="w-50" src="/images/produits/{{produit.image}}" style="align-self:center;" alt="Product_image"></div>
        <div class="col"><div class="d-flex justify-content-center align-center"><p class="d-flex text-center text-dark desc">{{produit.description}}</p></div>
           <div class="text-primary d-flex justify-content-end" style="font-weight: bold; font-size: 2rem;">{{produit.formattedPrice}} € </div>
           {% if produit.stock > 0 %}
                <div class="text-success d-flex justify-content-end" style="font-size: 2rem;">En stock</div>
                {% else %}
                <div class="text-danger d-flex justify-content-end" style="font-size: 2rem;">Hors stock</div>
                {% endif %}
        </div>
    </div>
    </div>   
    <div class="row py-4">
               <form class="d-flex" action="{{ path('app_panier_ajout' , {id: produit.id}) }}" method="post">
               <label for="quantite"></label>
                <select class="form-select" name="quantite" id="quantite">
                <option selected value="1">Choisissez la quantité</option>
                <option value="1">1</option>
                <option value="2">2</option>
                <option value="3">3</option>
                <option value="4">4</option>
                <option value="5">5</option>
                <option value="6">6</option>
                <option value="7">7</option>
                <option value="8">8</option>
                <option value="9">9</option>
                </select>
                <button type="submit align-self-right" class="btn btn-primary">Acheter</a>
                </div>
   </div>
{% endblock %}

